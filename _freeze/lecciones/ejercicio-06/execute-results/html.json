{
  "hash": "4ebc2de88fd5eaddaf44d922b7a374cd",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Ejercicio 6\"\nauthor:\n  - Camila Zugarramurdi\n  - Álvaro Cabana\ndate: \"2022\"\nlang: es\ncode-fold: false\ncode-tools: true\n---\n\n\n\n\n\n\n\n# Ejercicio 6: data-massage ancho vs largo\n\n\nEl **objetivo** del ejercicio 6 es familiarizarse con una de las partes más aburridas del data-massage, el llevar los datos de un formato a otro, sólo para poder hacer tal o cual análisis.\n\nVamos a trabajar con unos datos en formao SPSS, en formato ancho, y la ideas es convertirlos a formato largo, ya verán para qué.\n\nEn un segundo caso, tomaremos unos datos en formato largo, y los convertiremos en formato ancho para poder calcular correlaciones.\n\n\n## Empezamos\n\nVamos a trabajar con 2 archivos de datos; descárguenlos aquí: [archivo participantes1.sav](https://www.dropbox.com/s/7g8su4gh14o4j8s/participantes1.sav?dl=0), [animales.csv ](https://www.dropbox.com/s/p0volia7hdc6hpe/animales.csv?dl=0). Y guárdenlos en la carpeta _input_\n\n\n# Datos anchos\n\nLos datos provienen de un estudio enmarcado en el programa de investigación estudiantil PAIE, liderado por Valentina Paz y un grupo de estudiantes de grado e investigadores del CIBPsi, bajo la supervisión de Victoria Gradin. El estudio consistía en un hacer jugar a los participantes al _juego del ultimátum_. En este juego, una persona le ofrece al participante para compartir una determinada suma de dinero. La clave del asunto es que las ofertas pueden ser \"justas\" (del estilo $5 para ti, $5 para mi), \"injustas\" ($9 para mi, \\$1 para ti), o \"medias\" ($7 para mi, $3 para ti). Para trabajar en este ejemplo, vamos a ver las respuestas emocionales de los participantes ante cada tipo de oferta. Trabajaremos con la emoción \"enojo\", es decir, con las respuestas de los participantes, en una escala del 0 al 10, de qué tan enojados se sintieron ante ofertas justas, medias, e injustas. \n\n\n1) Cargar los contenidos del archivo _participante1.sav_, en un __data frame__ que se llame `parts`. \n\n::: {.cell}\n\n```{.r .cell-code}\n#si el paquete haven no está instalado, instalarlo con install.package(\"haven\")\nlibrary(haven) #por las dudas si no está cargado\nparts=read_sav(\"input/participantes1.sav\")\n```\n:::\n\n\nEl data frame obtenido es muy grande, por eso nos vamos a detener en algunas pocas variables. A saber:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\n\nparts2 = parts %>% \n  select(codigo, sexo, edad, Beck, contains(\"Enojo\"))\n\nstr(parts2)\n```\n:::\n\n\n2) Pasar el data frame al formato largo. Llamar a la variable categórica nueva \"tipo_oferta\", y a la numérica \"enojo\". Llamarle a ese data frame \"parts3\".\n\n\n3) Nombrar los niveles de la variable \"tipo_oferta\": \"justas\", \"injustas\", \"medias\".\n\n\n3) Yapa: ejecutar lo siguiente. Intentar averiguar qué hace. Prestar atención al ñoqui `~`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# aquí se ajusta un modelo lineal, donde la variable de respuesta (dependiente) es el enojo, \n# y la predictora (independiente) es el tipo de oferta.\nmodelo1=lm(enojo~tipo_oferta,data=parts3)\n# aquí se resume información variada relevante al modelo, como los valores de los parámetros,\n# pruebas de significancia sobre ellos, etc. Mirar estadístico t y p-valor (columna Pr(>|t|)).\nsummary(modelo1)\n# y aquí se hace un ANOVA con el modelo. En este caso, es de un factor (tipo de oferta), \n# con 3 niveles. Miren qué lindos el F y el p :)\nanova(modelo1)\n```\n:::\n\n\n\n4) Ensanchar el data frame largo al formato original.\n\nHermoso no?\n\n***\n\n#Datos largos\n\nEstos datos están tomados de la Norma de Producción de Rasgos Semánticos para el español rioplatense, de [Vivas et al., 2016](https://link.springer.com/article/10.3758/s13428-016-0777-2).\nA un buen número de hablantes nativos del español rioplatense, se les pidió que indicaran qué rasgos semánticos tiene cada palabra, para un total de 400 palabras. Aquí tenemos un resumen de palabras, todos nombres de animales, y algunos de los rasgos semánticos más relevantes.\n\n## Empecemos\n\n1) Cargar los datos del archivo \"animales.csv\" en un data frame llamado \"animales\".\n\n2) Eliminar la primera columna \"X.1\" del data frame.\n\n3) Nombrar la (ahora) primera columna \"X\" del data frame como \"nombre\".\n\n4) Llevar el data frame al formato ancho, con una fila por nombre, una columna por cada rasgo semántico. Nombrar a ese data frame \"animales2\".\n\n\n5) Llevar el data frame al formato ancho, con una fila por rasgo, una columna por cada nombre. Nombrar a ese data frame \"animales3\".\n\n\n6) Yapa: puedo calcular la correlación entre animales o rasgos semánticos usando los data frames anchos.\n\n::: {.cell}\n\n```{.r .cell-code}\ncor(animales2[,-1]) # Correlaciones entre todos los rasgos semánticos\ncor(animales3[,-1]) # Correlaciones entre todos los animales\n```\n:::\n\n\n***\n\n\n------------------------------------------------------------------------\n\n::: {.callout-tip .callout-solution collapse=\"true\"}\n## Soluciones\n\nDesplegá esta sección cuando quieras comparar con un conjunto de soluciones posibles.\n\nVamos a trabajar con unos datos en formato SPSS, en formato ancho, y la ideas es convertirlos a formato largo, ya verán para qué.\n\nEn un segundo caso, tomaremos unos datos en formato largo, y los convertiremos en formato ancho para poder calcular correlaciones.\n\n\n## Empezamos\n\nVamos a trabajar con 2 archivos de datos; descárguenlos aquí: [archivo participantes1.sav](https://www.dropbox.com/s/7g8su4gh14o4j8s/participantes1.sav?dl=0), [animales.csv ](https://www.dropbox.com/s/p0volia7hdc6hpe/animales.csv?dl=0). Y guárdenlos en la carpeta _input_\n\n\n# Datos anchos\n\nLos datos provienen de un estudio enmarcado en el programa de investigación estudiantil PAIE, liderado por Valentina Paz y un grupo de estudiantes de grado e investigadores del CIBPsi, bajo la supervisión de Victoria Gradin. El estudio consistía en un hacer jugar a los participantes al _juego del ultimátum_. En este juego, una persona le ofrece al participante para compartir una determinada suma de dinero. La clave del asunto es que las ofertas pueden ser \"justas\" (del estilo $5 para ti, $5 para mi), \"injustas\" ($9 para mi, \\$1 para ti), o \"medias\" ($7 para mi, $3 para ti). Para trabajar en este ejemplo, vamos a ver las respuestas emocionales de los participantes ante cada tipo de oferta. Trabajaremos con la emoción \"enojo\", es decir, con las respuestas de los participantes, en una escala del 0 al 10, de qué tan enojados se sintieron ante ofertas justas, medias, e injustas. \n\n\n1) Cargar los contenidos del archivo _participante1.sav_, en un __data frame__ que se llame `parts`. \n\n::: {.cell}\n\n```{.r .cell-code}\n#si el paquete haven no está instalado, instalarlo con install.package(\"haven\")\nlibrary(haven) #por las dudas si no está cargado\nparts=read_sav(\"input/participantes1.sav\")\n```\n:::\n\n\nEl data frame obtenido es muy grande, por eso nos vamos a detener en algunas pocas variables. A saber:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\n\nparts2 = parts %>% \n  select(codigo, sexo, edad, Beck, contains(\"Enojo\"))\n\nstr(parts2)\n```\n:::\n\n2) Pasar el data frame al formato largo. Llamar a la variable categórica nueva \"tipo_oferta\", y a la numérica \"enojo\". Llamarle a ese data frame \"parts3\".\n\n\n::: {.cell}\n\n```{.r .cell-code}\nparts3 = parts2 %>% \n  pivot_longer(cols = c(Feedback_Justas_Enojo,\n                        Feedback_Medias_Enojo,\n                        Feedback_Injustas_Enojo),\n               names_to = \"tipo_oferta\",\n               values_to = \"enojo\")\n\n# también podría haberlo hecho más elegantemente como:\nparts3 = parts2 %>% \n  pivot_longer(cols = contains(\"Enojo\"),\n               names_to = \"tipo_oferta\",\n               values_to = \"enojo\")\nhead(parts3)\n```\n:::\n\n\n\n3) Nombrar los niveles de la variable \"tipo_oferta\": \"justas\", \"injustas\", \"medias\".\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# lo convierto en factor\nparts3$tipo_oferta=factor(parts3$tipo_oferta)\n# miro los niveles\nlevels(parts3$tipo_oferta)\n# los renombro EN EL MISMO ORDEN\nlevels(parts3$tipo_oferta) = c(\"injustas\", \"justas\", \"medias\")\n# corroboro que no rompí nada\nhead(parts3)\n\n# podría haber hecho todo lo anterior en una sola línea como \n#(attenti que estoy usando el argumento LABELS, no el argumento LEVELS):\n\nparts3$tipo_oferta = factor(parts3$tipo_oferta, labels = c(\"injustas\", \"justas\", \"medias\"))\n```\n:::\n\n\n\n3) Yapa: ejecutar lo siguiente. Intentar averiguar qué hace. Prestar atención al ñoqui `~`.\n\n::: {.cell}\n\n```{.r .cell-code}\n# aquí se ajusta un modelo lineal, donde la variable de respuesta (dependiente) es el enojo, \n# y la predictora (independiente) es el tipo de oferta.\nmodelo1=lm(enojo~tipo_oferta,data=parts3)\n# aquí se resume información variada relevante al modelo, como los valores de los parámetros,\n# pruebas de significancia sobre ellos, etc. Mirar estadístico t y p-valor (columna Pr(>|t|))\nsummary(modelo1)\n# y aquí se hace un ANOVA con el modelo. En este caso, es de un factor (tipo de oferta), \n# con 3 niveles. Miren qué lindos el F y el p :)\nanova(modelo1)\n```\n:::\n\n\n4) Ensanchar el data frame largo al formato original.\n\n::: {.cell}\n\n```{.r .cell-code}\nparts3 %>% \n  pivot_wider(names_from = \"tipo_oferta\",\n              values_from = \"enojo\")\n```\n:::\n\n\n\nHermoso no?\n\n***\n\n#Datos largos\n\nEstos datos están tomados de la Norma de Producción de Rasgos Semánticos para el español rioplatense, de [Vivas et al., 2016](https://link.springer.com/article/10.3758/s13428-016-0777-2).\nA un buen número de hablantes nativos del español rioplatense, se les pidió que indicaran qué rasgos semánticos tiene cada palabra, para un total de 400 palabras. Aquí tenemos un resumen de palabras, todos nombres de animales, y algunos de los rasgos semánticos más relevantes.\n\n## Empecemos\n\n1) Cargar los datos del archivo \"animales.csv\" en un data frame llamado \"animales\".\n\n::: {.cell}\n\n```{.r .cell-code}\nanimales = read.csv('input/animales.csv',\n                    encoding = \"UTF-8\") # ojo con las carpetas\n```\n:::\n\n2) Eliminar la primera columna \"X.1\" del data frame.\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\nanimales = animales %>% \n  select(-X.1)\n         \n# hay varias formas de hacer esto, ésta es una de las más compactas. \n# Al poner un signo de menos delante de la columna, se quiere decir \n# \"todas las columnas MENOS ésta\". También se puede usar delante de c(). \n# Por ejemplo  [,-c(3,5)] selecciona todas las columnas MENOS la 3 y la 5. \n# También, [-c(8,9),-2] saca las filas 8 y 9 (enteras) y la columna 2 (entera).\n```\n:::\n\n\n3) Nombrar la (ahora) primera columna \"X\" del data frame como \"nombre\".\n\n::: {.cell}\n\n```{.r .cell-code}\nanimales = animales %>% \n  rename(nombre = X)\n```\n:::\n\n\n4) Llevar el data frame al formato ancho, con una fila por nombre, una columna por cada rasgo semántico. Nombrar a ese data frame \"animales2\".\n\n\n::: {.cell}\n\n```{.r .cell-code}\nanimales2 = animales %>% \n  pivot_wider(names_from = rasgo,\n              values_from = frecuencia)\n\nhead(animales2)\n```\n:::\n\n\n5) Llevar el data frame al formato ancho, con una fila por rasgo, una columna por cada nombre. Nombrar a ese data frame \"animales3\".\n\n\n::: {.cell}\n\n```{.r .cell-code}\nanimales3 = animales %>% \n  pivot_wider(names_from = nombre,\n              values_from = frecuencia)\n\nhead(animales3)\n```\n:::\n\n\n6) Yapa: puedo calcular la correlación entre animales o rasgos semánticos usando los data frames anchos.\n\n::: {.cell}\n\n```{.r .cell-code}\ncor(animales2[,-1]) # Correlaciones entre todos los rasgos semánticos\ncor(animales3[,-1]) # Correlaciones entre todos los animales\n```\n:::\n\n\n***\n\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}